---
# frontmatter = 给机器的元数据（check-final / 下游解析用）。闭集 12 字段，禁止新增。
# 尤其禁止 review_notes / unavailable —— 过程记录（修订史、偏差自省）属于正文 §7 溯源或 r*.md，不进 frontmatter。
task: "{{ 一句话任务（标题级，不是长哲学） }}"
mode: "{{ quick | full }}"
task_type: "{{ review | design | discuss | create }}"
models:
  lead: "{{ lead_model_name，如 claude }}"
  external: ["{{ 只列实际产出的外部模型，如 codex }}"]   # Codex 没跑通就别列、不要加 unavailable
run_id: "run-{{ YYYYMMDD-HHmm }}"
created_at: "{{ ISO8601 }}"
status: "active"                 # active | superseded
supersedes: null                 # 可选，默认 null（本版无自动版本链，需要时手动填前一个 run-id）
superseded_by: null              # 可选，默认 null
success_criteria: "{{ 单行摘要——这次 review 要回答什么；明细写进 §1，不要在这里堆多行 }}"
synthesis_bias_note: null        # 可选，≤1 句：主模型 R1 立场与最终裁决的偏差。默认 null
independent_review: null         # 可选，如 "pass"
---

# {{ 任务主题 }} — Cross Review 最终方案

## 给你

> 这一段 = 聊天收尾那段话的存档。人话、零代号（不要出现 R4 / D-N / 模型名 / Critical-Major / 方法论术语）。
> 想回溯细节再往下看；只想知道结论，看这一块就够。

**一句话决定**：{{ 做什么、不做什么 }}

**它能 / 不能帮你**（最关键的诚实话）：{{ 能做到什么、做不到什么 }}

**下一步**：
- [ ] {{ 动作 1 }}
- [ ] {{ 动作 2 }}

---
<以下为完整过程留痕，给想回溯的你 / 下游 AI / 未来优化流程的人看。这一层保留代号与来源锚点——它们是审计原料。>

## TL;DR

{{ 3-5 句结构化摘要，回答 任务 / 决策 / 下一步 / 取舍。给下游 AI 的精确版，可与"给你"呼应但保持完整句。 }}

## 1. 任务与约束

- **目标**：{{ 一句话 }}
- **关键约束**：{{ ≤3 条 }}
- **成功标准**：{{ 此处展开 frontmatter 下沉来的明细（如 7 个问题）}}

## 2. 核心分歧与裁决

> 每行一个议题：各方立场 → 裁决 → 依据。无分歧则写"无关键分歧"+ 列 R2 补充发现。

| # | 议题 | {{ Claude }} | {{ Codex }} | 裁决 | 依据 |
|---|------|------|------|------|------|
| D1 | {{ 议题 }} | {{ 一句立场 }} | {{ 一句立场 }} | **{{ 裁决 }}** | {{ R2 补充 / R4 攻击 N }} |

## 3. 最终方案

{{ 字段名按 task_type：review→"修复 N"，design→"方案要点 N"，discuss→"决策 N"，create→"结构要点 N"。 }}

### 决策 1：{{ 人话标题，不要用 "D-A·…" 这种代号当标题 }}

- **做什么**：{{ 可执行 / 可验证 }}
- **为什么**：{{ 与 §2 裁决呼应；人话依据 }}
- **来源**：{{ R3 候选#N / R4 修正#N —— 保留轮次锚点供溯源，这是溯源能力的钩子，不要删 }}

## 4. 被否决的替代路径

- **路径 X**（原主张：{{ model }}）：{{ 一句描述 }} → **否决理由**：{{ }}

（若无，写"无"）

## 5. 行动项

> P0 立即执行。P1/P2/P3 见 `action.md`（如生成）。纯分析任务写"无需行动"+理由。

### P0 — 立即执行

- [ ] **P0-1** {{ 动作 }}
  - 文件/位置：{{ src/xxx.ts:行号 或 "全局" }}
  - 来源：决策 1

## 6. 遗留风险与未决问题

- **风险 R1**：{{ 描述 }} — **缓解**：{{ 监控 / 兜底 / 接受 }}
- **未决问题 Q1**：{{ 描述 }} — **何时决定**：{{ 触发条件 }}

（若无，写"无"）

## 7. 附录：讨论脉络（含过程代号与修订史）

<details>
<summary>展开查看各轮发现摘要</summary>

- **R1 各方立场**：Claude → {{ 一句 }} / Codex → {{ 一句 }}
- **R2 交叉验证**：{{ 关键增量 }}
- **R2.5**（如触发）：{{ 一句 }}
- **R3 候选综合**：{{ 主题数 + 待检验假设数 }}
- **R4 对抗**（完备模式）：{{ 攻击点数 + 导致的修正数 }}
- **独立复核**（如做）：{{ reviewer + 结论 }}
- **修订史**（如有）：{{ 终审/复核导致的逐项修改记在这里——不要塞进 frontmatter 的 review_notes }}
- 原始交付物：`r1.*.md`、`r2.*.md`、`r3.synthesis.md`、`r4.*.md`

</details>

---

## 交付前自检（主模型）

> frontmatter 用脚本校验：`python3 scripts/cross_review_runtime.py check-final --file final.md`
> 正文结构靠主模型按下表自检（check-final 只解析 frontmatter，不碰中文正文）。

- [ ] frontmatter 8 必填齐全，且**无白名单外字段**（禁止 review_notes / unavailable）
- [ ] `## 给你` 区存在：人话、零代号，含"决定 / 能不能帮你 / 下一步"
- [ ] §2 分歧表 ≥ 1 行，或显式"无关键分歧"+ R2 补充
- [ ] 每个决策含"做什么 / 为什么 / 来源"
- [ ] §5 P0 ≥ 1 条，或"无需行动"+理由
- [ ] §6 显式填写（没有就写"无"）
- [ ] 总行数 ≤ 800（超出则细节下沉 action.md 或 §7）
- [ ] `models` 只列实际产出的模型，与聊天收尾里的降级声明一致
