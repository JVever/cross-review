---
task: "{{ 一句话任务描述 }}"
mode: "{{ quick | full }}"
task_type: "{{ review | design | discuss | create }}"
models:
  lead: "{{ lead_model_name }}"
  external: ["{{ external_1 }}", "{{ external_2 }}"]
run_id: "run-{{ YYYYMMDD-HHmm }}"
created_at: "{{ ISO8601 }}"
status: "active"                           # active | superseded
supersedes: null                           # null 或前一个 run-id
superseded_by: null                        # null 或后一个 run-id（后续 run 写入）
success_criteria: "{{ final.md 要回答什么问题——直接摘自任务对齐卡 }}"
synthesis_bias_note: null                  # 可选：主模型 R1 立场与裁决关系一句说明
independent_review: null                   # 可选：C1 独立 review 的结论（如 "pass" / "returned-for-rework"）
---

# {{ 任务主题 }} — Cross Review 最终方案

> **status: active** · mode: {{ mode }} · models: {{ lead }}+{{ external_1 }}+{{ external_2 }} · run: {{ run_id }}
>
> （如果本 run 被更新 run 取代，将在此处替换为"⚠️ 本 run 已被 run-xxx 取代，请查看最新结论"）

## TL;DR

{{ 3-5 句。必须回答四个问题：
  1. 任务是什么（1 句）
  2. 最终决策是什么（1-2 句）
  3. 下一步做什么（1 句，或指向 §5）
  4. 采纳 vs 否决的核心取舍（1 句）
读者读完这段应该能决定"是否继续往下看"。}}

## 1. 任务与约束

- **目标**：{{ 一句话目标 }}
- **关键约束**：
  - {{ 约束 1 }}
  - {{ 约束 2 }}
  - {{ 约束 3，如有 }}
- **成功标准**：{{ 直接摘自任务对齐卡——final.md 要回答的问题 }}

## 2. 核心分歧与裁决

> 每行记录一个议题：各方 R1 立场 → 裁决 → 依据。
> 如任务无分歧（如单纯描述性评审），改写"无关键分歧"并列举 R2 补充发现。

| # | 议题 | {{ Model A }} | {{ Model B }} | {{ Model C }} | 裁决 | 依据 |
|---|------|--------------|--------------|--------------|------|------|
| D1 | {{ 议题简述 }} | {{ 一句立场 }} | {{ 一句立场 }} | {{ 一句立场 }} | **{{ 采纳 B / 取折中 / 改造第三路径 }}** | {{ R2 补充 / R4 攻击 N / R2.5 裁决 }} |
| D2 | ... | ... | ... | ... | ... | ... |

## 3. 最终方案

{{ 对技术评审类任务，字段名可改"修复 N"；对设计类改"方案要点 N"；对创作类改"结构要点 N"。每个决策都必须填完下列全部子字段。 }}

### 决策 1：{{ 标题 }}

- **做什么**：{{ 具体描述，要可执行或可验证 }}
- **为什么**：{{ 与第 2 节裁决呼应；若为共识则说明各方均支持 }}
- **不做什么**：{{ 同时否决的路径；无则写"无" }}
- **来源**：{{ R3 候选 #N / R4 修正 #N / R2.5 裁决 / R1 共识 }}

### 决策 2：...

## 4. 被否决的替代路径

- **路径 X**（原主张者：{{ model_name }}）：{{ 一句描述 }} → **否决理由**：{{ ... }}
- **路径 Y**（原主张者：{{ model_name }}）：{{ ... }} → **否决理由**：{{ ... }}

（若无否决路径写"无"，说明各方方案互补而非互斥）

## 5. 行动项

> 本节列 P0（立即执行）。P1/P2/P3 行动见 `action.md`（若生成）。
> 若任务性质不产生行动项（如纯分析），显式写"无需行动"+理由。

### P0 — 立即执行

- [ ] **P0-1** {{ 动作描述 }}
  - 文件/位置：{{ src/xxx.ts:行号 或 "全局" }}
  - 依赖：{{ 无 / 依赖前置动作 }}
  - 来源：决策 1

- [ ] **P0-2** {{ 动作描述 }}
  - ...

## 6. 遗留风险与未决问题

- **风险 R1**：{{ 描述 }} — **缓解方式**：{{ 监控 / 兜底 / 接受 }}
- **未决问题 Q1**：{{ 描述 }} — **何时决定**：{{ 触发条件 / 时间节点 }}

（若无遗留风险与未决问题，显式写"无"）

## 7. 附录：讨论脉络

<details>
<summary>展开查看各轮发现摘要</summary>

- **R1 各方立场**：
  - {{ model_a }}：{{ 一句总结 }}
  - {{ model_b }}：{{ 一句总结 }}
  - {{ model_c }}：{{ 一句总结 }}
- **R2 交叉验证关键增量**：{{ 一句 }}
- **R2.5 焦点反驳**（如触发）：{{ 一句 }}
- **R3 候选综合**：{{ 主题数 + 待检验假设数 }}
- **R4 对抗性挑战**：{{ 攻击点数 + 导致的修正数 }}
- **独立 Final Review**（如有）：{{ reviewer model + 结论 }}

查看原始交付物：`r1.*.md`、`r2.*.md`、`r3.synthesis.md`、`r4.*.md`

</details>

---

## 硬校验清单（主模型交付前必须自检）

> 以下 8 项任何一项不通过，本 final.md 视为未完成，必须返工。
> 也可运行 `scripts/cross_review_runtime.py check-final --file final.md` 做结构化校验。

- [ ] frontmatter 8 个必填字段齐全（`task`/`mode`/`task_type`/`models`/`run_id`/`created_at`/`status`/`success_criteria`）
- [ ] TL;DR 存在且 ≥ 3 句，明确回答"任务/决策/下一步/取舍"四项
- [ ] 第 2 节分歧表 ≥ 1 行，或显式"无关键分歧"+ R2 补充清单
- [ ] 每个决策（§3）都含"做什么 / 为什么 / 来源"三子字段
- [ ] §5 P0 行动项 ≥ 1 条，或显式"无需行动"+ 理由
- [ ] §6 遗留风险字段显式填写（没有就写"无"）
- [ ] `task_type` 与模板骨架一致（review/design/discuss/create）
- [ ] 总行数 ≤ 800（超出说明应下沉 action.md 或附录）
